{
  "buildCommand": "npx prisma db push --force-reset --accept-data-loss && npx prisma generate && npx tsx prisma/seed.ts",
  "installCommand": "npm install",
  "functions": {
    "api/index.ts": {
      "memory": 256,
      "maxDuration": 30
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api/index.ts" }
  ]
}
